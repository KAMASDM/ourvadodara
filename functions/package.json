{
  "name": "functions",
  "description": "Cloud Functions for Our Vadodara News",
  "scripts": {
    "lint": "node --check index.js && node --check email.js && node --check registration-security.js && node --check scripts/backfill-public-projections.js",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.3.2",
    "nodemailer": "^9.0.5"
  },
  "private": true
}
